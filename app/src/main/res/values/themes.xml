<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PocketupDM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:fontFamily">@font/poppins</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorSecondaryContainer">@color/turquesa_oscuro</item>

    </style>

    <style name="Theme.PocketupDM" parent="Base.Theme.PocketupDM" />
    <color name="turquesa_dinamico">@color/turquesa</color>
    <color name="color_nav_indicador">@color/turquesa_oscuro</color>

    <!-- Colores del calendario -->
    <style name="Theme.PocketUpDM" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    </style>

    <style name="Theme.App.Calendar.Turquesa" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="colorPrimary">@color/turquesa_oscuro</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>
</resources>